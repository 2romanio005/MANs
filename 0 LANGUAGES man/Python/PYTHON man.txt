list()	[] — списки
tuple()	() — кортежи;
str()	"" — строки;
set()	{} — изменяемые множества;
frozenset()— неизменяемые множества;
dict()	{key: val} — словари (ассоциативные массивы, хеш таблицы).


@blabla - похожи на аннотации (возможно декорация)