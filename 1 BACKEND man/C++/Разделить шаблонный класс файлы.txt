инклудим .cpp в конце .h, и меняем тип файла с cpp на загаловок h, чтобы он не компилился

template class name<usedType>; // так пишем во всех cpp файлах, содержащих реализацю шалона, для всех используемых типов